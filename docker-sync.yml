version: '2'
options:
  verbose: true
syncs:
  timelapseapp-ui-src-sync:
    src: './'
    dest: '/app'
    sync_excludes: ['.git', 'data', '.idea', 'node_modules', '*.pyc']
    sync_host_port: 10892
